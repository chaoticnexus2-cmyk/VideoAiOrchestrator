# API Lambda runtime dependencies — fully pinned, including transitives.
#
# Install with the manylinux platform flags so the native wheels (awscrt, ijson)
# match the Lambda runtime instead of the build machine. Use
# scripts/build-api-lambda.ps1 rather than a bare `pip install`.
#
# Direct dependencies
# ------------------
# boto3 is pinned rather than relying on the Lambda-provided version, because the
# bundled runtime lags behind the Bedrock and Transcribe APIs this handler calls.
boto3==1.43.3
# Nova Sonic needs the bidirectional streaming client, which ships separately
# from boto3.
aws-sdk-bedrock-runtime==0.5.0

# Transitive dependencies — pinned so builds are reproducible
# ----------------------------------------------------------
aws-sdk-signers==0.2.0
awscrt==0.32.2
botocore==1.43.90
ijson==3.5.1
jmespath==1.1.0
python-dateutil==2.9.0.post0
s3transfer==0.17.1
six==1.17.0
smithy-aws-core==0.5.0
smithy-aws-event-stream==0.2.2
smithy-core==0.4.0
smithy-http==0.4.4
smithy-json==0.2.3
urllib3==2.7.0
